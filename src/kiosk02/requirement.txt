Lv 2. 객체 지향 설계를 적용해 햄버거 메뉴를 클래스로 관리하기
    요구사항이 가지는 의도
    객체 지향 개념을 학습하고,
    데이터를 구조적으로 관리하며 프로그램을 설계하는 방법을 익힙니다.
    햄버거 메뉴를 MenuItem 클래스와 List를 통해 관리합니다.

    MenuItem 클래스 생성하기
    개별 음식 항목을 관리하는 클래스입니다.
    현재는 햄버거만 관리합니다.
    클래스는 이름, 가격, 설명 필드를 갖습니다.
    main 함수에서 MenuItem 클래스를 활용하여 햄버거 메뉴를 출력합니다.
    MenuItem 객체 생성을 통해 이름, 가격, 설명을 세팅합니다.

    키워드: new
    List를 선언하여 여러 MenuItem을 추가합니다.

    List<MenuItem> menuItems = new ArrayList<>();
    반복문을 활용해 menuItems를 탐색하면서 하나씩 접근합니다.

    구조 예시
    public static void main(String[] args) {
		// List 선언 및 초기화
		// add 함수를 통해 new MenuItem(이름, 가격, 설명) List에 삽입

		// Scanner 선언

		// 반복문을 활용해 List 안에 있는 MenuItem을 하나씩 출력
		// 숫자를 입력 받기
		// 입력된 숫자에 따른 처리
			// 프로그램을 종료
			// 선택한 메뉴 : 이름, 가격, 설명
}

list 방식으로 선언하는 방법?
List<MenuItem> menuItems = new ArrayList<>();
arraylist 사용하기

** 리스트에 추가한 메뉴를 하나씩 출력하는 방식
-> 향상된 for문을 이용해서
println하면 하나씩 출력할 수 있음.
-> 해당 클래스에서 toString을 통해 재정의하는 과정이 필요함.
-> 번호를 매기기 위해서 for문 외부에서 i 변수를 추가하고 i++; 로직 작성
-> i를 굳이 외부에서 추가하는 방식을 사용한 이유는 향상된 for문을 쓰고 싶어서.



  [ SHAKESHACK MENU ]
    1. ShackBurger   | W 6.9 | 토마토, 양상추, 쉑소스가 토핑된 치즈버거
    2. SmokeShack    | W 8.9 | 베이컨, 체리 페퍼에 쉑소스가 토핑된 치즈버거
    3. Cheeseburger  | W 6.9 | 포테이토 번과 비프패티, 치즈가 토핑된 치즈버거
    4. Hamburger     | W 5.4 | 비프패티를 기반으로 야채가 들어간 기본버거
    0. 종료      | 종료
위 메뉴를 //
add 함수를 통해 new MenuItem(이름, 가격, 설명) List에 삽입 하라는 것 같음
이런식으로 직접적으로 메뉴를 입력해야 하는 경우, 생성자가 필요한 것 같음
안 넣으면 인수 관련 오류 발생함.
추가로 가격이 6.9처럼 실수형이니까 double로 바꿔줘야함
추가로 price값이 존재하지 않을때는 price를 생략하는 코드를 추가해줘야할듯함
-> 종료의 경우를 고려해야함
-> toString 구문에서 0일경우에 대한 유효성 검사 코드를 추가했음!
계속 실행을 해보기는 했는데 입력값을 넣어보진 않다가
여기까지 구현하고 입력값을 넣어봤는데
가격에 5.5를 넣으니 inputmismatchexception 오류 발생.
생각해보니 double형인데 입력값 받아올때, sc.nextInt라고해서 발생한 문제
-> double로 변경
-> 이후 다시 입력했는데
-> 이제는 가격까지는 잘 입력이 되는데 가격 입력 이후, 설명부분 입력을 건너뛰어버림.
-> 아마 이전 과제에서도 발생한 버퍼문제일듯함.
-> 맞았음. 가격 입력부분 이후 버퍼를 한 번 비우니까 해결됐음.
-> 그리고 반복문 내에서 입력과 메뉴추가가 끝났으니 전체 메뉴를 출력함.
-> 정상적으로 구현이 끝난 것 같음
-> level2 끝.

